; AM reference-photo uploads (phone JPEGs commonly exceed PHP's 2M default).
; Install on EC2: sudo cp deployment/99-am-uploads.ini /etc/php.d/ && sudo systemctl restart php-fpm
upload_max_filesize = 10M
post_max_size = 12M
