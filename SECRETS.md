# Secrets Location

PEM key files and other credentials for this project are stored in:

```
/Users/mattmso/Dropbox/AI Projects/Secrets/
```

Files moved there:
- `1pwrAM.pem` — EC2 SSH key
- `nexus_key_pair.pem` — Nexus portal key

When docs reference paths like `ssh -i 1pwrAM.pem`, the key file lives in the Secrets directory above, not in this repo.
